<%= form_for [@course, @comment] , :html => { :multipart => true } do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">

    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= button_tag(type: 'submit', class: 'btn btn-sm btn-primary', value: 'Add Comment') do %>
      Add Comment
    <% end %>
  </div>
<% end %>
